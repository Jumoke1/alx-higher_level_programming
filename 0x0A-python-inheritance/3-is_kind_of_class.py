#!/usr/bin/python3
'''A function that retrns true if obj is an instance of an inherited  class.'''


def is_kind_of_class(obj, a_class):
    '''Check if an onject is an instance or inherited instance.

    Args:
         obj (any): The object
         a_class (type): The class to match thhe obj
    Return:
         if obj is exactly an instance return true
         Otherwise - False.
    '''
    if isinstance(obl, a_class):
        return True
    return False
